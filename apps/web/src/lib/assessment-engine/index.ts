/**
 * Re-export everything from @changepath/engine.
 * This file preserves backward compatibility with existing relative imports
 * throughout the web app (e.g., `from '../lib/assessment-engine'`).
 */
export * from '@changepath/engine';
